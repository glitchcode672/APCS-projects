
class Lesson_34_Activity_Four 
{
       public static int findMin(int [] a)
    {
         int small = a[0];
         for(int i = 0; i < a.length; i++)
         {
           if(small > a[i])
             small = a[i];
         }
         return small; 
    }
}
